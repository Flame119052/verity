# Local development only — for `npm run dev` in the source checkout
# Production/installed use reads from ~/Library/Application Support/VERITY/config.json instead
VAULT_PATH=/path/to/your/Obsidian Vault
PORT=4477
NODE_ENV=development
